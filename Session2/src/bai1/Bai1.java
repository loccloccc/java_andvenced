package bai1;

public class Bai1 {
    static void main(String[] args) {
//        Trong **Java**, các công việc tương ứng với Functional Interface như sau:
//
//          Kiểm tra xem một User có phải Admin hay không (trả về true/false)** → **Predicate<User>** vì Predicate dùng để kiểm tra điều kiện và trả về giá trị boolean.
//
//          Chuyển đổi một đối tượng User thành chuỗi String chứa username** → **Function<User, String>** vì Function dùng để chuyển đổi dữ liệu từ một kiểu sang kiểu khác.
//
//          In thông tin chi tiết của User ra màn hình Console** → **Consumer<User>** vì Consumer nhận dữ liệu để xử lý nhưng không trả về kết quả.
//
//          Khởi tạo một đối tượng User mới với các giá trị mặc định** → **Supplier<User>** vì Supplier không nhận tham số nhưng trả về một đối tượng.

    }
}
